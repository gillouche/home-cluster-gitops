apiVersion: apps/v1
kind: Deployment
metadata:
  name: postgres
  namespace: playground-infra-test
  labels:
    app: postgres
    environment: test
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: postgres
  template:
    metadata:
      labels:
        app: postgres
        environment: test
    spec:
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      nodeSelector:
        workload.tier: infra
      containers:
        - name: postgres
          image: nexus.gillouche.homelab/docker-hub/library/postgres:18.3-alpine
          ports:
            - containerPort: 5432
          envFrom:
            - secretRef:
                name: test-postgres-credentials
          volumeMounts:
            - name: data
              mountPath: /var/lib/postgresql
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "1Gi"
              cpu: "1"
          livenessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 30
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - pg_isready
                - -U
                - postgres
            initialDelaySeconds: 5
            periodSeconds: 5
            timeoutSeconds: 3
            failureThreshold: 3
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop: ["ALL"]
      volumes:
        - name: data
          hostPath:
            path: /mnt/data/playground-infra/test/postgres
            type: DirectoryOrCreate
---
apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: playground-infra-test
spec:
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
