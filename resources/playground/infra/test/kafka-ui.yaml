apiVersion: apps/v1
kind: Deployment
metadata:
  name: kafka-ui
  namespace: playground-infra-test
  labels:
    app: kafka-ui
    environment: test
spec:
  replicas: 1
  selector:
    matchLabels:
      app: kafka-ui
  template:
    metadata:
      labels:
        app: kafka-ui
        environment: test
    spec:
      nodeSelector:
        workload.tier: infra
      containers:
        - name: kafka-ui
          image: kafbat/kafka-ui:v1.4.2
          ports:
            - containerPort: 8080
          env:
            - name: KAFKA_CLUSTERS_0_NAME
              value: "playground-test"
            - name: KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS
              value: "kafka.playground-infra-test.svc.cluster.local:9092"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: kafka-ui
  namespace: playground-infra-test
spec:
  selector:
    app: kafka-ui
  ports:
    - port: 8080
      targetPort: 8080
