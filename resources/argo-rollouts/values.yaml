controller:
  replicas: 2
  resources:
    requests:
      cpu: 15m
      memory: 128Mi
    limits:
      memory: 256Mi
  nodeSelector:
    node-class: homeserver
  tolerations:
    - key: "node-role.kubernetes.io/control-plane"
      operator: "Exists"
      effect: "NoSchedule"
dashboard:
  enabled: true
  resources:
    requests:
      cpu: 15m
      memory: 128Mi
    limits:
      memory: 256Mi
  nodeSelector:
    node-class: homeserver
  ingress:
    enabled: true
    ingressClassName: traefik
    hosts:
      - rollouts.k3s.gillouche.homelab
    annotations:
      traefik.ingress.kubernetes.io/router.entrypoints: web
      traefik.ingress.kubernetes.io/router.middlewares: security-authelia@kubernetescrd
